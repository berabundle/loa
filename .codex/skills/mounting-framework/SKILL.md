---
name: mounting-framework
description: Wrapper for Loa skill "mounting-framework". Use when you need the Loa workflow for this role.
---
Use the Loa skill instructions at `.claude/skills/mounting-framework/SKILL.md`.

- Read the entire Loa skill file before acting.
- Follow its workflows, constraints, and output requirements.
- Never edit `.claude/` directly; use `.claude/overrides/` or `.loa.config.yaml` if changes are required.
